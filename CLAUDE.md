@AGENTS.md

## Claude Code compatibility

`AGENTS.md` is the canonical Agent Needle instruction source. This file exists
only so Claude Code loads the same repository instructions.

Do not duplicate lifecycle, execution, memory, or safety rules here. Update
`AGENTS.md` instead.
